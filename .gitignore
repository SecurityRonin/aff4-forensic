/target/
/fuzz/target/
# Fuzz corpora are large binary test data — gitignored, kept locally / regenerated.
/fuzz/corpus/
/fuzz/corpus_reference/
# Build/report artifacts (mkdocs site, coverage output) — never committed.
/site/
lcov.info
